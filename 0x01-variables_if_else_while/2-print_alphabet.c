#include <stdio.h>
/**
 * main - print the alphabet in lowercase letters
 *
 * Return: 0
*/
int main(void)
{
	char letter:

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);
	printf("\n")
	return (0);
}
